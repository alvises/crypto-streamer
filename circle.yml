machine:
  python:
    version: 3.6

dependencies:
  pre:
    - sudo apt-get update
    - pip install -r requirements.txt
    - pip install -r requirements-testing.txt


test:
    override:
        - pytest tests